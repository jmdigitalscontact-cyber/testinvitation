; Shared hosting (cPanel) picks this up for PHP-FPM / CGI.
; Needed so compressed guest POV uploads are not rejected by the tiny default 2M limit.
upload_max_filesize = 12M
post_max_size = 16M
max_file_uploads = 50
memory_limit = 512M
